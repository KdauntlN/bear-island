[package]
name = "client"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 3

[dependencies]
bevy = "0.19.1"
common = { path = "../common" }
wincode = {version = "0.6.1", features = ["derive"] }