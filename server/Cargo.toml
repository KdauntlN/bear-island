[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common/" }
wincode = {version = "0.6.1", features = ["derive"]}
tokio ={ version = "1.53.1", features = ["derive"] }